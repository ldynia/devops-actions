name: CI
on:
  push:
    branches: [ actions ]
  workflow_dispatch:
    branches: [ actions ]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Run a one-line script
        run: |
          echo Hello, world 1!
          echo Hello, world 2!